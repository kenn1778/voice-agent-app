# Agent Runtime
FastAPI container on Bedrock AgentCore handling bidirectional audio streaming (Nova Sonic) and digest generation (Nova Pro).

## Local Dev
1. `python -m venv venv && source venv/bin/activate`
2. `pip install -r requirements.txt`
3. `uvicorn app.main:app --reload`
